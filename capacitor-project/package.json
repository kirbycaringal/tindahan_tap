{
  "name": "tindahan-pos",
  "version": "1.0.0",
  "private": true,
  "description": "Offline sari-sari store POS and inventory app",
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  }
}
